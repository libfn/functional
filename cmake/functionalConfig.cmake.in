@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/functionalTargets.cmake")

# pfn is always installed
set(functional_pfn_FOUND TRUE)

# fn is only installed when the library was built without DISABLE_CXX23
if(TARGET functional::fn)
    set(functional_fn_FOUND TRUE)
endif()

check_required_components(functional)
